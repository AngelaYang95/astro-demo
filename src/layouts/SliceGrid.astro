<div class="grid">
    <slot />
</div>
<style lang="scss">
    @import '../styles/mixins.scss';
    
    .grid {
        display: grid;
        grid-template-columns: repeat(12, 1fr);
        margin: 0 auto;
        max-width: 1920px;
        padding: 0 64px;
        @include tablet {
            grid-template-columns: repeat(12, 1fr);
            padding: 0 32px;
        }
        @include mobile {
            grid-template-columns: repeat(4, 1fr);
            padding: 0 32px;
        }
    }
</style>