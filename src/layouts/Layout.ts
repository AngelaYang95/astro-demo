
const MOBILE_MAX = 480;
const TABLET_MAX = 800;
const DESKTOP_MAX = 1020;

const MQ = {
    sm: `@media (max-width: ${MOBILE_MAX}px)`,
    md: `@media (max-width: ${TABLET_MAX}px)`,
    lg: `@media (max-width: ${DESKTOP_MAX}px)`,
}